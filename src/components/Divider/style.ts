import styled from "styled-components/native";
import { DividerProps } from "./type";
import { Divider } from "@rneui/themed";

export const DividerStyled = styled(Divider).attrs<DividerProps>(
  (props) => ({})
)<DividerProps>``;
